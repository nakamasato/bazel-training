name: proto

on:
  pull_request:
    paths-ignore:
      - "**.md"
      - ".github/**"
      - "!.github/workflows/proto.yaml"


jobs:
  proto:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          bazel run //:gazelle
          bazel build //proto
